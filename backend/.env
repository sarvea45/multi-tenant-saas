# Database (Matches docker-compose service name 'database')
DB_HOST=database
DB_PORT=5432
DB_NAME=saas_db
DB_USER=postgres
DB_PASSWORD=postgres

# JWT Secret (Test value for evaluation)
JWT_SECRET=super_secret_key_must_be_long_12345
JWT_EXPIRES_IN=24h

# Server
PORT=5000
NODE_ENV=development

# Frontend URL (Matches docker-compose service name 'frontend')
FRONTEND_URL=http://localhost:3000